# CryptoView — Техническое задание

## 1. Общее описание

**CryptoView** — система мониторинга криптовалютных ордербуков для обнаружения аномальных плотностей (крупных лимитных заявок) на множестве бирж.

### 1.1 Цели проекта
- Мониторинг стаканов (orderbook) на 8 криптобиржах
- Обнаружение аномальных плотностей по двум алгоритмам
- Отправка алертов в Telegram
- Гибкая система настроек (глобальные, по бирже, по типу рынка, по монете)

### 1.2 Биржи и типы рынков

| Биржа | Spot | Futures | Котировочная валюта |
|-------|------|---------|---------------------|
| Binance | ✅ | ✅ | USDT |
| Bybit | ✅ | ✅ | USDT |
| OKX | ✅ | ✅ | USDT |
| Bitget | ✅ | ✅ | USDT |
| Gate | ✅ | ✅ | USDT |
| MEXC | ✅ | ❌ | USDT |
| Hyperliquid | ✅ | ✅ | USDC |
| Lighter | ✅ | ✅ | USDT |

**Примечание:** MEXC Futures исключён из мониторинга.

---

## 2. Функциональные требования

### 2.1 Мониторинг ордербуков

#### 2.1.1 Подключение к биржам
- WebSocket соединения для real-time данных
- Автоматический реконнект при обрыве связи
- Подписка на все доступные торговые пары
- Максимальная доступная глубина стакана от каждой биржи

#### 2.1.2 Фильтрация данных
- Анализируются только уровни в пределах **10% от текущей цены**
- Если биржа предоставляет меньше 10% глубины — используется всё доступное

#### 2.1.3 Сбор объёмов
- Агрегация торговых объёмов за последние **15 минут** (скользящее окно)
- Отдельно для каждой комбинации: биржа + тип рынка + монета

### 2.2 Детекция аномалий

#### 2.2.0 Общий алгоритм фильтрации (пошагово)

При каждом обновлении стакана система проходит следующие шаги для **каждого уровня цены**:

```
Шаг 1: Предварительный фильтр (min-density-usd)
│   Объём уровня < minDensityUsd?
│   ├─ ДА → уровень ОТБРОШЕН, дальше не проверяется
│   └─ НЕТ → переходим к Шагу 2
│
Шаг 2: Проверка VOLUME_BASED (если включён)
│   Объём уровня > volume15min?
│   ├─ ДА → проверка дедупликации → если не дубликат → АЛЕРТ
│   └─ НЕТ → ничего
│
Шаг 3: Проверка STATISTICAL (если включён, независимо от Шага 2)
│   Объём уровня > statisticalThreshold?
│   ├─ ДА → проверка дедупликации → если не дубликат → АЛЕРТ
│   └─ НЕТ → ничего
```

**Важно:** `min-density-usd` — это входной порог-фильтр. Если уровень его не прошёл, ни VOLUME_BASED, ни STATISTICAL проверки не выполняются. Если прошёл — обе проверки выполняются **независимо друг от друга** и могут сгенерировать **два отдельных алерта** на один и тот же уровень.

#### 2.2.0.1 Разрешение значения min-density-usd (каскад приоритетов)

Значение `minDensityUsd` определяется по каскадной иерархии (первое найденное не-null):

```
1. symbols-config.json  (конкретная монета, наивысший приоритет)
2. application.yml      (marketType: spot/futures для биржи)
3. application.yml      (exchange: уровень биржи)
4. application.yml      (global: глобальное значение, самый низкий приоритет)
```

**Пример для BTCUSDT на Binance Futures:**
- symbols-config.json: BTCUSDT.minDensityUsd = 100,000,000 → **используется $100M**
- (Если бы не было в symbols-config → Binance Futures: $600,000)
- (Если бы не было для Futures → Binance: не задано)
- (Если бы не было для Binance → global: $100,000)

**Пример для мелкого альта XYZUSDT на Gate Spot:**
- symbols-config.json: не задан → пропуск
- Gate Spot: $400,000 → **используется $400,000**

#### 2.2.1 Тип 1: VOLUME_BASED (по торговой стратегии)
Плотность считается аномальной, если:
```
orderSizeUSD > volume15minUSD
```
Где:
- `orderSizeUSD` — объём лимитной заявки в USD
- `volume15minUSD` — суммарный проторгованный объём за 15 минут

**Пример:** SOL на Bybit Futures. За 15 минут проторговано $5M. В стакане появился ордер на $18M. $18M > $5M → VOLUME_BASED алерт. Но если minDensityUsd для SOL = $15M, а ордер $3M → $3M < $15M → уровень отброшен на Шаге 1, до этой проверки не дойдёт.

#### 2.2.2 Тип 2: STATISTICAL (статистический)
Используется комбинация методов для определения аномалий:

**Z-score метод:**
```
μ = среднее значение ордеров в стакане
σ = стандартное отклонение
Z = (orderSize - μ) / σ
isAnomaly = |Z| > 3
```

**IQR метод (более устойчив к выбросам):**
```
Q1 = 25-й перцентиль размеров ордеров
Q3 = 75-й перцентиль
IQR = Q3 - Q1
upperBound = Q3 + 3 × IQR
isAnomaly = orderSize > upperBound
```

**Итоговый порог:** берётся `min(Z-score threshold, IQR threshold)` для более строгой фильтрации.

**Пример:** Стакан BTC на OKX, 200 уровней. Медиана ордеров ~$30K, стандартное отклонение ~$50K. Z-score threshold = $30K + 3×$50K = $180K. Если minDensityUsd для BTC = $100M, то все ордера сначала проверяются на Шаге 1: ордер $180K < $100M → **отброшен**, STATISTICAL проверка не выполняется. Только ордера > $100M дойдут до статистической проверки.

#### 2.2.3 Дедупликация алертов

Ключ дедупликации: `{exchange}_{marketType}_{symbol}_{side}_{точная_цена}_{alertType}`

Cooldown: настраиваемый (по умолчанию 5 минут). Если алерт с таким же ключом уже отправлялся в пределах cooldown-периода, повторный алерт не отправляется.

**Ограничение:** если цена сдвигается даже на $0.1 — это новый уникальный ключ, и алерт будет отправлен повторно.

### 2.3 Система алертов

#### 2.3.1 Содержание алерта
- Биржа и тип рынка
- Символ (торговая пара)
- Тип алерта (VOLUME_BASED / STATISTICAL)
- Сторона (BID / ASK)
- Цена уровня
- Объём плотности в USD
- Расстояние от текущей цены (%)
- Объём за 15 минут (для контекста)
- Комментарий к монете (если есть)

#### 2.3.2 Дедупликация
- Не отправлять алерт на ту же плотность повторно
- Cooldown период для одной монеты (настраиваемый, по умолчанию 5 минут)

#### 2.3.3 Telegram интеграция
- Отправка через Telegram Bot API
- Поддержка форматирования (Markdown)
- Возможность указать несколько chat_id

### 2.4 Система настроек

#### 2.4.1 Иерархия настроек (от общего к частному)
1. **Глобальные** — применяются ко всем
2. **По бирже** — переопределяют глобальные для конкретной биржи
3. **По типу рынка** — переопределяют настройки биржи для spot/futures
4. **По монете** — переопределяют все вышестоящие для конкретной монеты

#### 2.4.2 Параметры настроек
| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `minDensityUsd` | Минимальный объём плотности для алерта | 100,000 |
| `enabled` | Включен ли мониторинг | true |
| `alertTypes` | Типы алертов (VOLUME_BASED, STATISTICAL) | оба |
| `cooldownMinutes` | Пауза между алертами для одной монеты | 5 |
| `maxDistancePercent` | Макс. расстояние от цены для анализа | 10.0 |

#### 2.4.3 Комментарии к монетам
- Каждой монете можно присвоить комментарий (до 200 символов)
- Комментарий отображается в алертах

#### 2.4.4 Экспорт/Импорт настроек
- Выгрузка всех настроек в JSON
- Загрузка настроек из JSON файла
- Валидация при импорте

---

## 3. Нефункциональные требования

### 3.1 Производительность
- Обработка до 50,000 сообщений/сек от бирж
- Латентность детекции < 100ms
- Потребление RAM < 4 GB

### 3.2 Надёжность
- Автоматический реконнект при обрыве WebSocket
- Graceful shutdown с сохранением состояния
- Логирование всех ошибок

### 3.3 Масштабируемость
- Архитектура позволяет добавлять новые биржи
- Возможность горизонтального масштабирования в будущем

---

## 4. Технический стек

### 4.1 Backend (Фаза 1)
- **Язык:** Java 21
- **Фреймворк:** Spring Boot 3.x
- **Сборщик:** Gradle (Groovy DSL)
- **WebSocket клиент:** Java-WebSocket / OkHttp
- **HTTP клиент:** OkHttp / WebClient
- **JSON:** Jackson
- **Telegram:** Telegram Bots API

### 4.2 Frontend (Фаза 2)
- **Фреймворк:** React 18+
- **Язык:** TypeScript
- **UI библиотека:** Tailwind CSS / Ant Design
- **State management:** Zustand / React Query
- **WebSocket:** native / Socket.IO client

---

## 5. API бирж

### 5.1 Binance
- **Spot WebSocket:** `wss://stream.binance.com:9443/ws`
- **Futures WebSocket:** `wss://fstream.binance.com/ws`
- **Orderbook stream:** `<symbol>@depth@100ms`
- **Глубина:** до 5000 уровней (snapshot), diff updates

### 5.2 Bybit
- **Spot WebSocket:** `wss://stream.bybit.com/v5/public/spot`
- **Futures WebSocket:** `wss://stream.bybit.com/v5/public/linear`
- **Orderbook stream:** `orderbook.50.<symbol>` или `orderbook.200.<symbol>`
- **Глубина:** 50 или 200 уровней

### 5.3 OKX
- **WebSocket:** `wss://ws.okx.com:8443/ws/v5/public`
- **Orderbook channel:** `books` или `books50-l2-tbt`
- **Глубина:** до 400 уровней

### 5.4 Bitget
- **Spot WebSocket:** `wss://ws.bitget.com/spot/v1/stream`
- **Futures WebSocket:** `wss://ws.bitget.com/mix/v1/stream`
- **Orderbook channel:** `books15` или `books`
- **Глубина:** 15 или полный стакан

### 5.5 Gate
- **Spot WebSocket:** `wss://api.gateio.ws/ws/v4/`
- **Futures WebSocket:** `wss://fx-ws.gateio.ws/v4/ws/usdt`
- **Orderbook channel:** `spot.order_book` / `futures.order_book`
- **Глубина:** до 100 уровней

### 5.6 MEXC (только Spot)
- **WebSocket:** `wss://wbs.mexc.com/ws`
- **Orderbook channel:** `spot@public.limit.depth.v3.api@<symbol>@20`
- **Глубина:** 5, 10, 20 уровней

### 5.7 Hyperliquid
- **WebSocket:** `wss://api.hyperliquid.xyz/ws`
- **Orderbook:** L2 book через subscription
- **Особенности:** Только USDC пары, ограниченная бесплатная глубина

### 5.8 Lighter
- **WebSocket:** Требует изучения актуальной документации
- **Примечание:** Менее популярная биржа, API может отличаться

---

## 6. Структура конфигурации

### 6.1 application.yml (основной конфиг)
```yaml
cryptoview:
  telegram:
    bot-token: "YOUR_BOT_TOKEN"
    chat-ids:
      - "123456789"

  global:
    min-density-usd: 100000
    cooldown-minutes: 5
    max-distance-percent: 10.0
    alert-types:
      - VOLUME_BASED
      - STATISTICAL

  exchanges:
    binance:
      enabled: true
      spot:
        enabled: true
        min-density-usd: 150000
      futures:
        enabled: true
        min-density-usd: 200000
```

### 6.2 symbols-config.json (настройки монет)
```json
{
  "symbols": {
    "BTCUSDT": {
      "comment": "Основная монета, высокая ликвидность",
      "minDensityUsd": 500000,
      "enabled": true
    },
    "ETHUSDT": {
      "comment": "Вторая по капитализации",
      "minDensityUsd": 300000
    }
  }
}
```

---

## 7. Формат алерта в Telegram

```
🔴 АНОМАЛЬНАЯ ПЛОТНОСТЬ

📊 BTC/USDT | Binance Futures
📈 Тип: VOLUME_BASED

💰 Объём: $1,250,000
📍 Цена: 65,432.50 (ASK)
📏 От рынка: -2.3%

📊 Объём 15м: $890,000
⏱ Время: 14:32:15 UTC

💬 Комментарий: Основная монета, высокая ликвидность
```

---

## 8. Фазы разработки

### Фаза 1: Backend + Telegram (текущая)
- Полный бэкенд на Java/Spring Boot
- Все коннекторы к биржам
- Детекция аномалий
- Telegram алерты
- Конфигурация через файлы

### Фаза 2: Frontend
- React приложение
- Дашборд с монетами
- Настройки через UI
- Real-time обновления через WebSocket

---

## 9. Риски и ограничения

### 9.1 Rate Limits
- Каждая биржа имеет лимиты на подключения
- Необходимо соблюдать ограничения, иначе бан IP

### 9.2 Hyperliquid
- Бесплатная версия имеет ограниченную глубину стакана
- Полный стакан платный

### 9.3 Lighter
- Менее документированный API
- Возможны сложности с интеграцией

### 9.4 Объём данных
- При мониторинге всех монет — высокая нагрузка
- Рекомендуется VPS с хорошим интернет-каналом
